O(n) time, O(1) space
class Solution(object):
    def missingNumber(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        
        size = len(nums)
        res = size
        for idx in range(size):
            res ^= idx^nums[idx]
        return res
