O(lgn) time, O(1) space
class Solution(object):
    def minSteps(self, n):
        """
        :type n: int
        :rtype: int
        """
        
        res, d = 0, 2
        while d <= n:
            while n%d == 0:
                res += d
                n /= d
            d += 1
        return res
