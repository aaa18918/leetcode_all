class Solution(object):
    def isPowerOfFour(self, num):
        """
        :type num: int
        :rtype: bool
        """
        
        return num == 4**int(math.log(num, 4)) if num > 0 else False
