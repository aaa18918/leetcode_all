O(n) time, O(1) space
class Solution(object):
    def climbStairs(self, n):
        """
        :type n: int
        :rtype: int
        """
        
        record = [1, 2]
        for i in range(2, n):
            record[i%2] = record[0]+record[1]
        
        return record[(n-1)%2]
