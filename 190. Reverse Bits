O(32) time, O(1) space
class Solution:
    # @param n, an integer
    # @return an integer
    def reverseBits(self, n):
        res = 0b0
        
        for _ in range(32):
            res = (res<<1) + n%2
            n /= 2
        
        return res
