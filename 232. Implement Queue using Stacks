O(1) time, 0(1) space push; O(n) time, O(n) space pop; O(1) time, O(1) space peek; O(1) time, O(1) space empty; Overall O(n) space
class MyQueue(object):

    def __init__(self):
        """
        Initialize your data structure here.
        """
        
        self.stack = []

    def push(self, x):
        """
        Push element x to the back of queue.
        :type x: int
        :rtype: None
        """
        
        self.stack.append(x)

    def pop(self):
        """
        Removes the element from in front of queue and returns that element.
        :rtype: int
        """
        
        tmp_stack = []
        for _ in range(len(self.stack)-1):
            tmp_stack.append(self.stack.pop())
        
        res = self.stack.pop()
        
        while tmp_stack:
            self.stack.append(tmp_stack.pop())
        
        return res

    def peek(self):
        """
        Get the front element.
        :rtype: int
        """
        
        return self.stack[0]

    def empty(self):
        """
        Returns whether the queue is empty.
        :rtype: bool
        """
        
        return not self.stack

# Your MyQueue object will be instantiated and called as such:
# obj = MyQueue()
# obj.push(x)
# param_2 = obj.pop()
# param_3 = obj.peek()
# param_4 = obj.empty()
