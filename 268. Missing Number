O(n) time, O(1) space
class Solution(object):
    def missingNumber(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        
        res = len(nums)
        for idx in range(len(nums)):
            res ^= idx^nums[idx]
        
        return res
