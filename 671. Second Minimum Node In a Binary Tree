O(n) time, O(n) space
# Definition for a binary tree node.
# class TreeNode(object):
#     def __init__(self, x):
#         self.val = x
#         self.left = None
#         self.right = None

class Solution(object):
    def findSecondMinimumValue(self, root):
        """
        :type root: TreeNode
        :rtype: int
        """
        
        if not root:
            return -1
        
        my_stack = [root]
        self.res = -1
        while my_stack:
            node = my_stack.pop()
            if root.val < node.val and (self.res == -1 or node.val < self.res):
                self.res = node.val
            
            if node.val == root.val:
                if node.left:
                    my_stack.append(node.left)
                
                if node.right:
                    my_stack.append(node.right)
        
        return self.res


还有一些别的解法没有看完。
