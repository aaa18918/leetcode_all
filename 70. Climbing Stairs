O(n) time, O(1) space
class Solution(object):
    def climbStairs(self, n):
        """
        :type n: int
        :rtype: int
        """
        
        a, b = 1, 2
        for _ in range(n-2):
            a, b = b, a+b
        
        return b if n >= 2 else a
