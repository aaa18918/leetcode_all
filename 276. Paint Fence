O(n) time, O(1) space
class Solution(object):
    def numWays(self, n, k):
        """
        :type n: int
        :type k: int
        :rtype: int
        """
        
        a, b = 0, k
        for _ in range(n-1):
            a, b = b, (a+b)*(k-1)
        
        return a+b if n > 0 else 0
